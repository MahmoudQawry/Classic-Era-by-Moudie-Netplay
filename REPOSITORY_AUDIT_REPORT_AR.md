# تقرير التدقيق الشامل لمستودع Classic Era by Moudie Netplay

**تاريخ التدقيق:** 13 سبتمبر 2026

## الخلاصة التنفيذية

تم تحليل بنية المشروع، مسارات Android وExpo، خادم الغرف، Socket.IO، LiveKit، مزامنة المحاكيات، الترجمة، الاختبارات، الاعتماديات، ملفات الإصدارات وأصول المشروع. المشروع قابل للبناء، وفحص TypeScript والاختبارات الحالية نجحا. كما نجح بناء APK Release أثناء هذه الجولة.

تدعم بنية التطبيق أربعة أنظمة فقط: **PS1 وNES وPSP وSEGA**. نوى ARM64 الأربعة موجودة في حزمة Android المبنية. تم التحقق من عدم وجود المصطلحين المحظورين المرتبطين بالمنتجات المنافسة في ملفات المصدر الحالية. كما أزيلت مخلفات الإصدارات القديمة من APK من شجرة المشروع، مع إبقاء إصدار Release الحالي للتوزيع.

الملاحظة الأهم ليست فشلًا في البناء، بل حد إنتاجي: مسار الصوت يعتمد الآن على LiveKit، إلا أن إصدار رموز LiveKit يتم في خادم Node.js. لذلك يجب تشغيل خادم Backend فعلي بالمتغيرات السرية الصحيحة؛ لا يجوز وضع السر داخل APK. اختبار الاتصال الصوتي بين شبكات عالمية، واتصال بيانات الهاتف، وBluetooth والمكالمات الهاتفية لا يمكن إثباته من بيئة البناء وحدها.

## نطاق التدقيق

شمل التدقيق مجلدات `app` و`components` و`lib` و`server` و`modules` و`tests` و`scripts` و`docs`، إضافة إلى إعدادات Expo وAndroid وGitHub Actions وملف الاعتماديات. بلغ عدد الملفات التي جرى جردها داخل المسارات الرئيسية ذات العمق المحدد 158 ملفًا، بينما يتضمن مجلد المشروع ملفات توليد وبناء واعتماديات كبيرة لا ينبغي اعتبارها مصدرًا يدويًا.

| المجال | النتيجة |
|---|---|
| الأنظمة المدعومة | PS1، NES/Famicom، PSP، SEGA |
| Arcade/MAME في المصدر التشغيلي | غير موجودين بعد التنظيف الحالي |
| LiveKit | مدمج لمسار الصوت، مع token يصدره الخادم |
| WebRTC Mesh اليدوي | أزيل من مكون الصوت الأساسي |
| Socket.IO | مستخدم للغرف، الحضور، الدردشة وإشارات NetPlay |
| TypeScript | ناجح |
| الاختبارات | 76 ناجحًا، 2 متجاوزان عمدًا |
| ESLint | لا أخطاء، مع 6 تحذيرات React Hook |
| Android Release | نجح البناء |
| أسرار LiveKit | محلية ومستثناة من Git، وليست داخل المستودع |

## المعمارية الحالية

تستخدم الواجهة Expo Router وReact Native. يستخدم الخادم Express وtRPC وDrizzle، بينما يستخدم Socket.IO لقنوات الغرف والمزامنة. يمر الصوت من خلال `LiveKitRoom`، ويحصل التطبيق على token من مسار `rooms.mediaToken` بعد التحقق من بيانات عضوية الغرفة.

يحتوي خادم NetPlay على تحقق من العضوية، حدود للمعدل، حدود لحجم الرسائل، نقل WebSocket، استعادة اتصال Socket.IO، تاريخ محدود لإطارات الإدخال، تفاوض لتأخير الإدخال، والتحقق من تطابق بصمة اللعبة وإصدار النواة قبل بدء الجلسة. هذه عناصر مفيدة، لكنها لا تعادل خادم محاكاة authoritative كاملًا يحتفظ بحالة قابلة لإعادة البناء لكل إطار؛ ما زال استقرار اللعب الفعلي يعتمد على تطابق النواة والملف والشبكة والأجهزة.

## الصوت الجماعي

مسار الصوت الحالي أفضل من WebRTC Mesh السابق من ناحية قابلية التوسع؛ فـ LiveKit يتولى غرفة الوسائط والاتصال بين المشاركين، بينما يبقى Socket.IO لمسائل الحالة النصية والحضور. يتحقق الخادم من عضوية الغرفة قبل إصدار token، ويحدد هوية العضو واسم الغرفة والدور داخل claims موقعة.

يوجد شرط تشغيلي لا يمكن تجاوزه: يجب أن يعمل Backend الذي يحتوي `LIVEKIT_API_KEY` و`LIVEKIT_API_SECRET` في بيئة خادم. ملف `.env.local` المحلي مستثنى من Git، ولا يكفي وحده لتشغيل نسخة APK عند اتصالها بخادم منشور لا يحمل المتغيرات نفسها. يجب تدوير المفتاح والسر اللذين ظهرا سابقًا في المحادثة قبل أي نشر عام.

## مزامنة اللعب

تم العثور على آليات تحقق من `frame`، وحدود للإطارات القديمة والمستقبلية، وتاريخ إدخالات محدود، وبصمات ROM/core، وحواجز جاهزية، وطلبات حالة وإعادة مزامنة. هذه الآليات تقلل حالات التراكم والتعارض، لكنها لا تسمح بادعاء أن المزامنة العالمية مثبتة دون اختبار جهازين حقيقيين.

الاختبارات الآلية لا تحاكي زمن شبكة حقيقيًا أو فقدان حزم فعليًا أو NAT صعبًا أو إيقاف التطبيق أثناء اللعب. لذلك يجب اعتبار الاختبار الميداني على هاتفين مرحلة قبول لازمة، وليس بديلًا عن اختبارات Vitest.

## الأمن والاتصال

قائمة CORS لا تسمح بالأصول العامة افتراضيًا؛ فهي تسمح بالأصول المحددة في `ALLOWED_ORIGINS`، وتسمح بالمضيف المحلي أثناء التطوير إذا لم يتم ضبط القائمة. يجب ضبط `ALLOWED_ORIGINS` صراحة في بيئة الإنتاج وعدم استخدام `*` مع credentials.

يتم التحقق من عضوية Socket.IO عبر room/member token قبل قبول جلسة NetPlay. توجد حدود لحجم Socket.IO ومعدل إدخال اللاعبين. مع ذلك، لا ينبغي تشغيل خادم الإنتاج بالاعتماد على ذاكرة عملية واحدة إذا كان المطلوب تشغيل عدة نسخ؛ يلزم مخزن مشترك أو الالتزام بنسخة واحدة مع إدارة نشر واضحة.

## الاعتماديات

أعاد `pnpm audit --prod` حالة غير صفرية، مع 1079 اعتمادًا في الشجرة، ونتيجة مجمعة قدرها 6 منخفضة و29 متوسطة و73 عالية و2 حرجة. معظم العدد يقع في شجرة Expo وأدوات البناء، لكن التقرير يتضمن أيضًا حزمًا ذات صلة بالخادم مثل `drizzle-orm` و`express`/`qs` و`undici` و`ws` حسب مسار الاعتماد.

لم أطبق تحديثًا عشوائيًا شاملًا؛ لأن Expo وReact Native وLiveKit وAndroid native مرتبطة بنسخ متوافقة، والتحديث الأعمى قد يكسر البناء أو المحاكيات. التوصية هي تصنيف الثغرات إلى تشغيلية في Backend وثغرات أدوات بناء، ثم تحديث كل مجموعة مع تشغيل الاختبارات والبناء بعد كل مجموعة. يجب إعطاء الأولوية لأي حزمة تصل إليها بيانات خارجية في خادم الإنتاج، وعدم مساواة ثغرة أداة بناء لا تدخل APK بثغرة في مسار HTTP العام.

## الترجمة والتنظيف

اجتازت اختبارات الحماية الخاصة بالترجمة. توجد تحذيرات Hook مرتبطة بإدراج دالة `t` في بعض dependency arrays، لكنها ليست أخطاء بناء. ينبغي إصلاحها في جولة مستقلة لأن تغيير لغة التطبيق أثناء دورة حياة الشاشة قد يترك callback قديمًا في بعض المسارات.

تم تنظيف المراجع النصية الحالية للمصطلحات المنافسة والمراجع التشغيلية القديمة للنظام المحذوف. كما أزيلت APKs القديمة من شجرة المشروع لتقليل الحجم ومنع الخلط بين نسخ Debug وRelease. يبقى أي ملف ترحيل قاعدة بيانات تاريخي جزءًا من سجل schema؛ حذفه عشوائيًا قد يسبب فشلًا عند قواعد بيانات سبق تطبيق الترحيل عليها، ولذلك يجب التعامل معه كأثر تاريخي لا كميزة قابلة للوصول من التطبيق.

## نتائج التحقق

تم تشغيل الأوامر التالية بنجاح:

```text
pnpm run check
pnpm test
pnpm run lint

git diff --check
./gradlew assembleRelease --no-daemon --max-workers=2
```

نتيجة الاختبارات: 24 ملف اختبار ناجح، و76 اختبارًا ناجحًا، واختباران متجاوزان عمدًا. نتيجة lint: لا توجد أخطاء، مع ستة تحذيرات React Hook. نتيجة Gradle: `BUILD SUCCESSFUL`.

## التوصيات قبل النشر العام

أولًا، تدوير LiveKit API key وsecret اللذين ظهرا في المحادثة، ثم وضع القيم الجديدة في بيئة Backend المنشورة فقط. ثانيًا، ضبط `ALLOWED_ORIGINS` صراحة. ثالثًا، إجراء اختبار قبول على هاتفين بملفات لعبة قانونية متطابقة عبر Wi‑Fi ثم Wi‑Fi إلى بيانات الهاتف. رابعًا، قياس RTT وفقدان الحزم والانحراف الإطاري أثناء اللعب، وتسجيل حالات resync. خامسًا، معالجة الثغرات التشغيلية المباشرة قبل ترقية أدوات Expo غير المؤثرة في APK. سادسًا، إصلاح تحذيرات React Hook قبل تجميد واجهة الإصدار العام.

## الإصدار الناتج

تم بناء APK Release جديد ورفعه إلى GitHub Release بعد نجاح التحقق. رابط الإصدار موجود في رسالة التسليم، ويجب تثبيته على أجهزة الاختبار مع تذكر أن توفر الصوت العالمي يعتمد على Backend منشور ومضبوط ببيانات LiveKit الجديدة.

## المراجع

[1]: https://github.com/MahmoudQawry/Classic-Era-by-Moudie-Netplay "مستودع Classic Era by Moudie Netplay"
[2]: https://docs.npmjs.com/auditing-package-dependencies-for-security-vulnerabilities "npm audit documentation"
[3]: https://docs.livekit.io/transport/self-hosting/ports-firewall/ "LiveKit ports and firewall documentation"
